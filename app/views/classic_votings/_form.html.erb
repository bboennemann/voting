<%= form_for(@classic_voting) do |f| %>
  <% if @classic_voting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classic_voting.errors.count, "error") %> prohibited this classic_voting from being saved:</h2>

      <ul>
      <% @classic_voting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
