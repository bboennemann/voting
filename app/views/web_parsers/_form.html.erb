<%= form_for(@web_parser) do |f| %>
  <% if @web_parser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_parser.errors.count, "error") %> prohibited this web_parser from being saved:</h2>

      <ul>
      <% @web_parser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
