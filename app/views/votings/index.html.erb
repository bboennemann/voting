<div class="row">
	<div class="col-md-12">
		<div id="v_tile_container">
		<% @votings.each_with_index do |voting, i| %>
			<div class="grid class">
					<%= render "/#{voting.voting_type}_votings/tiles/#{voting.item_type}", :voting => voting, :i => i %>
			</div>
		<% end %>
		</div>
	</div>
</div>
